

/**
 * Write a description of class DistanceConverter here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class DistanceConverter
{
    /** description of instance variable x (add comment for each instance variable) */
    private int x;

    /**
     * Default constructor for objects of class DistanceConverter
     */
    public DistanceConverter()
    {
      public static void main(String[] args)
      
      final static double infot= 12;
      final static double ftyd=3
      double yards = 3.5;
      double feet = yards * INFOT
      double inches = feet * FTYD;
      
      System.out.println(yards + "yards are" + feet + "feet");
      System.out.println(yards + "yards are" + inches + "inches");
      } 
}
